<!DOCTYPE html>
<html>
    <head>
      <title>C-Debt system</title>
      <link rel="apple-touch-icon" href="<%= asset_path('apple-touch-icon-precomposed.png') %>" />
      <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
      <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
      <%= csrf_meta_tags %>
      <meta name="viewport" content="width=device-width, initial-scale=1">

    </head>
    <body>
    <div class="navbar <%= @side_color %> noMargin">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-warning-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand fontJedi" href="/">C debt</a>
            <div class="app_version">v0.5.1</div>
        </div>
        <div class="navbar-collapse collapse navbar-warning-collapse">
            <ul class="nav navbar-nav">
            <% if current_user != nil %>
                <li>
                    <%= link_to t('Your debts'), user_debts_path(current_user.id) %>
                </li>
                <li>
                    <%= link_to t('Your purchases'), user_purchases_path(current_user.id) %>
                </li>
            <% end %>

            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li>
                <% if current_user != nil %>
                    <h3 class="user_name">Hello <%= current_user.name %></h3>
                <% end %>

                </li>
<!--                 <li class="dropdown">
                    <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown">
                        <% if I18n.locale == :es %>
                        ES
                        <% else %>
                        EN
                        <% end %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <% if I18n.locale == :es %>
                            <li><a href="javascript:selectLanguage('en')">EN</a></li>
                        <% else %>
                            <li><a href="javascript:selectLanguage('es')">ES</a></li>
                        <% end %>
                    </ul>
                </li> -->
            </ul>
        </div>
    </div>
    <%= yield %>

    <script>
        $(function() {
            $.material.init();
        });
        function selectLanguage (language)
        {
            $.ajax({
              url: <%= #language_path
              %> + language,
              success: function () {
                location.reload();
              }
            });
        }
    </script>

    </body>
</html>
